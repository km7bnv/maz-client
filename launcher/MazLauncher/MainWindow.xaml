<Window x:Class="MazLauncher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MazLauncher" Height="430" Width="720" WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="#0F172A">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <Border Width="48" Height="48" Background="#5865F2" CornerRadius="9" Margin="0,0,14,0">
                <TextBlock Text="M" Foreground="White" FontSize="30" FontWeight="Bold"
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="MazLauncher" Foreground="White" FontSize="30" FontWeight="Bold"/>
                <TextBlock Text="Minecraft 26.2 — Vanilla or MazClient" Foreground="#94A3B8" FontSize="14" Margin="0,6,0,0"/>
            </StackPanel>
        </StackPanel>

        <Border Grid.Row="1" Background="#111827" CornerRadius="10" Margin="0,22,0,0" Padding="16">
            <DockPanel>
                <StackPanel DockPanel.Dock="Left">
                    <TextBlock x:Name="AccountText" Text="Not signed in" Foreground="White" FontSize="15" FontWeight="SemiBold"/>
                    <TextBlock Text="Uses Microsoft/Xbox authentication" Foreground="#94A3B8" FontSize="12" Margin="0,4,0,0"/>
                </StackPanel>
                <Button x:Name="SignInButton" DockPanel.Dock="Right" Content="Sign in with Microsoft" Width="170" Height="34" Click="SignInButton_Click"/>
            </DockPanel>
        </Border>

        <Grid Grid.Row="2" Margin="0,22,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="18"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" x:Name="VanillaButton" Click="VanillaButton_Click" Padding="20" Background="#1E293B" BorderBrush="#334155">
                <StackPanel>
                    <TextBlock Text="VANILLA" Foreground="White" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <TextBlock Text="Clean Minecraft 26.2" Foreground="#CBD5E1" Margin="0,8,0,0" HorizontalAlignment="Center"/>
                    <TextBlock Text="No MazClient mods" Foreground="#94A3B8" Margin="0,4,0,0" HorizontalAlignment="Center"/>
                </StackPanel>
            </Button>

            <Button Grid.Column="2" x:Name="MazButton" Click="MazButton_Click" Padding="20" Background="#5865F2" BorderBrush="#7C83FF">
                <StackPanel>
                    <TextBlock Text="MAZCLIENT" Foreground="White" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <TextBlock x:Name="MazVersionText" Text="Checking MazClient version..." Foreground="White" Margin="0,8,0,0" HorizontalAlignment="Center"/>
                    <TextBlock Text="Fabric API + cloud updates" Foreground="#E2E8F0" Margin="0,4,0,0" HorizontalAlignment="Center"/>
                </StackPanel>
            </Button>
        </Grid>

        <StackPanel Grid.Row="3" Margin="0,18,0,0">
            <ProgressBar x:Name="Progress" Height="8" Minimum="0" Maximum="100" Value="0"/>
            <TextBlock x:Name="StatusText" Text="Ready" Foreground="#CBD5E1" FontSize="12" Margin="0,8,0,0"/>
        </StackPanel>
    </Grid>
</Window>
