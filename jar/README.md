# Tested MazClient builds

Only major builds that have been manually tested by the user belong in this folder.

When a major build is confirmed working in-game, archive that exact compiled JAR here with a versioned/tested filename. Do not archive CI-only or untested builds.
